---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven

inputs:
  - name: assets
  - name: ermintrude

outputs:
  - name: target

run:
  path: ermintrude/ci/scripts/compile.sh
